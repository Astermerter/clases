#include <iostream>
#include "head.h"
using namespace std;


int main()
{
	
	Lamp lp;

	/*lp.setForce(15.1);
	lp.setPower(11.1);
	lp.setCondition(1);
	cout << lp.getForce() << " " << lp.getPower() << " " << lp.getCondition();*/

	cout << test(1, 10, 0);
	return 0;
} 